
export const Mapa  = (props) => {

    return(
        <div>
            <h2 tabIndex={0} style={{fontSize: '26px', color: '#1D3E43', paddingLeft: '80px'}}>{props.titulo}</h2>
            EN PROCESO :v
        </div>
    )
}